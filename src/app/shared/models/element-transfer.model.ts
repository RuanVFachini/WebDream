export class ElementTransfer {
    element: Node;
    description: string;

    constructor(element: Node, description: string) {
        this.element = element;
        this.description = description;
        
    }
}